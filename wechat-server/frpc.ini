# frp configuration for WeChat server

[common]
# frp server address (replace with your frp server)
server_addr = your-frp-server.com
server_port = 7000

# Authentication (if enabled)
# token = your-auth-token

[wechat]
type = tcp
local_ip = 127.0.0.1
local_port = 3000
remote_port = 6000
